# postman.prerequest

Add pre-request scripts that run before each Postman request.

## Usage

```properties
postman.prerequest=console.log('Sending request...');
```

This rule uses merge mode — multiple values will be accumulated.
