# field.order

Set field display order (lower values appear first).

## Usage

```properties
field.order=groovy:it.name() == "id" ? 0 : 100
```

## See Also

- [field.order.with](/settings/rules/field_order_with) — Set field order by comparator
- [Field Order Extensions](/framework/field-order) — Built-in ordering extensions
