# regex

Regular expression utilities. Alias: `RE`

## Usage

```groovy
regex.matches(text, pattern)
regex.findFirst(text, pattern)
regex.findAll(text, pattern)
regex.replace(text, pattern, replacement)
```
